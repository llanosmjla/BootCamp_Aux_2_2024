// BootCamp_Aux_2_2024.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

#include "Dibujador.h"
#include "Digito.h"
#include "Manejador.h"


/*
int main()
{

	vector<Digito*> digitos;

	Manejador* manejador = new Manejador();

	/* Dibujador* dibujo = new Dibujador();
	 dibujo->dibujar(0);
	 cout << "=======" << endl;
	 dibujo->dibujar(1);
	 cout << "=======" << endl;
	 dibujo->dibujar(2);*/

	 /*int numDigito = 1;
	 char secuenciaDig[7] = { 'b','c' }; // 1
	 string sec = "bc"; // 1

	int numDigito = 2;
	//char secuenciaDig[7] = { 'a','b','g','e','d' }; // 2
	//string sec = "abged"; // 2

	Digito num1Digito(numDigito);
	cout << "=======" << endl;
	//Digito num2Digito(secuenciaDig);
	cout << "=======" << endl;
	//Digito num3Digito(sec);

	// Instanciar un objeto de la clase Digito
	//Digito* digito = new Digito();

	Digito* digito = new Digito({'c', 'b'});
	Digito* digito2 = new Digito("fgbc"); // 4
	Digito* digito3 = new Digito("cb"); // 1

	// Agregar el objeto a un vector de punteros a Digito

	digitos.push_back(digito);
	digitos.push_back(digito2);
	digitos.push_back(digito3);

	// Llamar a la funcion dibujar de la clase Digito para dibujar el digito

	cout << "Dibujar digito: " << endl;
	for (int i = 0; i < digitos.size(); i++) {
		digitos[i]->dibujar();
		cout << "=======" << endl;
	}

	cout << "=======" << endl;

	// Llamar a la funcion aDigito de la clase Digito para convertir la secuencia a su respectivo digito
	//int valor = digito->aDigito(sec);

	// Siguiente valor
	//cout << "Siguiente valor: " << endl;
	//manejador->siguiente(valor);

	// Liberar memoria

	delete manejador;
	delete digito;


	return 0;
}*/